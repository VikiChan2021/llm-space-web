# Web 助手弹窗与仓库展示 V1 验收记录

- 验收日期：2026-08-18
- 功能提交：`9eb8452996efef72efdab8f08839096a85e84a4d`
- 腾讯云发布：`20260818-145005-9eb8452996ef`
- 线上地址：`https://kandian.site/llm-space-web/#/workbench`

## 功能结果

1. Threads 删除确认框中的助手入口进入 Footer 正常布局，取消、删除、助手三项均可见可点击；实际删除后 Thread 数量从 2 变为 1。
2. 弹窗内展开助手后只显示标题、当前面板上下文、消息、输入和安全说明；不显示 Weather Track、surface 说明卡或快捷操作。
3. 学习引导可以独立关闭，刷新后仍保持关闭；“开启引导”可以恢复，Track 进度不被删除。
4. Settings/MCP、Variables、Tools、Examples、Threads 与重置确认均保留可达助手入口，且不覆盖宿主操作。
5. 390×844 下紧凑助手完整位于视口内，页面 `scrollWidth` 与 `innerWidth` 均为 390。

## 验证结果

- Coach 聚焦测试：20 pass / 0 fail。
- `mise run check:changed`：通过。
- `mise run build:guest-web`：通过；保留仓库既有 top-level await 与大 chunk 容忍警告。
- `mise run pack:guest-api`：通过。
- 完整仓库测试：822 pass / 1 skip / 25 fail；失败集中于 Windows 缺少 `/bin/sh`/`python3`、symlink 权限、POSIX 路径/权限和生成文件换行基线。
- GitHub Linux CI：成功。
- 腾讯云：Nginx 配置成功、服务 active、Models 与 Quota API 200。
- 生产浏览器：0 error / 0 warning；桌面与移动视口无页面级横向溢出。

## 截图

- `01-online-guidance-closed-persisted.png`：线上刷新后学习引导仍关闭，助手保留按需问答与恢复入口。
- `02-online-delete-actions-unblocked.png`：线上删除确认框三个 Footer 操作入口无重叠。
- `03-online-dialog-compact-coach.png`：线上删除确认框内紧凑助手，仅保留对话核心。
- `04-online-settings-launcher.png`：线上设置弹窗标题区助手入口与关闭按钮互不覆盖。
- `05-online-mobile-dialog-compact-coach.png`：线上 390×844 重置确认框内紧凑助手。

## 已知边界

- GitHub Pages workflow 在 fork 未启用 Pages 时于 `configure-pages` 返回 Not Found；当前生产入口是腾讯云，不宣称该静态发布成功。
- 此轮未改变 Guest API 的模型、工具权限或额度策略，也未新增高影响页面动作。
